<div class="row">
  <div class="small-12 columnw">
    <h2>Create a new player</h2>

    <p class="lead">
      Use the following form to create a new player for the game.
    </p>

    <p>
      If have already created a player, you don't need to create a new one.
      Just go to <%= link_to 'new game', new_game_path %> and select your player from
      the dropdown.
    </p>
  </div>
</div>


<div class="row">
  <div class="columns small-12">
    <%= form_for(@player) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <div class="row">
        <div class="columns small-6">
          <%= f.label :name, 'Player Name' %>
          <%= f.text_field :name, required: true, minlength: 3, maxlength: 100 %>
        </div>
        <div class="columns small-6">
          <%= f.submit 'Create new player', class: 'expand success button radius' %>
        </div>
      </div>
    <% end %>
  </div>
</div>


